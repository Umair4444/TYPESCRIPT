"use strict";
// function left_right() {
//   for (let i = 0; i <= 100; i++) {
//     let output = "";
//     if (i % 3 == 0 && i % 5 == 0) output += "both";
//     else if (i % 3 == 0) output += "left";
//     else if (i % 5 == 0) output += "right";
//     console.log(output || i);
//   }
// }
// left_right();
let myString = "Hello";
let myarray = ["car", "bike", "plane", "1234567"];
let i = 0;
for (let char of myarray) {
    // in console char is printed
    // char => variable to store iteration
    // myarray [index] if is given it will iterate each char seperately and if w/o index iterate whole array to the length of it
    // in console.log
    // my array w/o index will take its array as length & with inde will take its element index as it length
    // myarray => works as final condition or length of iteration
    // if i console myarray[index] it will iterate same index element
    // console.log(char);
    // console.log(myarray[i]);
    // i++;
    console.log(char);
}
